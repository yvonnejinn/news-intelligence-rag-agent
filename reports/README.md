# Evidence inventory

`verification.json` records the executed offline checks. `smoke_*` and `demo_*` files describe fictional fixtures only. They are not real project benchmark scores. Optional integrations listed as unexecuted must be tested with real data and credentials before claims are upgraded. Exact local dependency versions are recorded in verification.json; fresh installs may resolve different versions within pyproject.toml ranges.
